# 仙途 · 项目接管文档

> 生成日期：2026-08-06
> 用途：让新对话（新 AI 会话/开发者）在**不读旧对话**的情况下，快速完整接管「仙途」文字修仙游戏项目并继续开发。
> 新接手者请务必通读本文件 + `深化文档.md` + `深化文档2.md` + `功能总览.md`，再动手改代码。

---

## 一、项目一句话概述

「仙途」是一款**手机优先的 PWA 文字修仙游戏**（纯前端静态站点，无后端），已完成多轮深化迭代，功能覆盖角色创建、修炼、突破、战斗、宗门、人际/道侣、副业、秘境、轮回、宗门经营、子女传承、天道终局等 20+ 系统。当前已部署到 GitHub Pages 线上可玩，带邀请码门禁。

- **线上地址**：https://zhou-ck.github.io/xiantu/
- **邀请码**：`xiantu2026`（改 `js/core/gate.js` 的 `INVITE_CODE` 即可换；留空 `''` 则完全开放）
- **仓库**：https://github.com/Zhou-ck/xiantu （远端 `main` 分支，已同步）
- **本地路径**：`F:\trae-work\trae-play\仙途2`

---

## 二、项目结构与架构

### 2.1 目录结构

```
仙途2/
├── index.html              # 唯一入口页（脚本加载顺序见 2.3，严禁乱改）
├── manifest.json           # PWA 清单
├── sw.js                   # Service Worker（离线缓存，改版本号触发更新）
├── css/main.css            # 全部样式（移动端优先，dvh/safe-area）
├── assets/
│   ├── portraits/          # 角色立绘 61 张（出身/道侣/NPC/宗门人物/灵兽）
│   ├── scenes/             # 场景背景 14 张
│   └── LICENSES.md         # 素材授权台账（续补素材时必须登记）
├── icons/                  # PWA 图标 3 套
├── js/
│   ├── core/               # utils / world(世界观数据) / content(事件库) / persona(出身)
│   │                       # narratives(心魔叙事) / gate(邀请码) / state(游戏状态) / save
│   ├── systems/            # mechanics / quests / npc / cultivation / breakthrough / explore
│   │                       # combat / dungeon / market / sect / social / romance / craft
│   │                       # rest / freeact / death
│   ├── ui/                 # ui / tabs / create / fx / settings / reward / audio / help
│   ├── api/ai-bridge.js    # AI 叙事桥（玩家自接 API，无 key 自动回退本地规则版）
│   └── app.js              # 启动入口
├── test/                   # 测试体系（见第六节）
├── 深化文档.md             # 第一版深化路线（含大量待办，别删）
├── 深化文档2.md            # 第二版深化蓝本（2A-2V + 3A-3G，大部分已实现）
├── 功能总览.md             # 当前功能全盘点（更新功能后建议同步）
└── 部署指南.md             # GitHub Pages / Netlify / Cloudflare 部署与访问控制
```

### 2.2 技术栈

- 纯原生 HTML/CSS/JS，无构建工具、无 npm 依赖（`test/` 仅用 Node 内置模块）
- PWA：`manifest.json` + `sw.js`（缓存全部资源，离线可玩，可添加到主屏幕）
- 存档：localStorage 3 个档位 + 全局账户（轮回点/轮回值跨档）
- 兼容形态：网页 PWA；`../仙途-app` 为预留的 Capacitor App 打包骨架（未启用）
- 素材：本地 jpg 图片（见 `assets/LICENSES.md` 授权台账）

### 2.3 脚本加载顺序（铁律）

`index.html` 中 `<script src>` 顺序即运行时依赖顺序，**新增脚本必须插入正确位置，不能打乱已有顺序**：

```
core/utils → core/world → core/content → core/persona → core/narratives
→ core/gate → core/state → systems/mechanics → systems/quests → systems/npc
→ ui/ui → ui/tabs → ui/create → ui/fx → ui/settings → ui/reward → ui/audio
→ systems/cultivation → systems/breakthrough → systems/explore → systems/combat
→ systems/dungeon → systems/market → systems/sect → systems/social
→ systems/romance → systems/craft → systems/rest → systems/freeact
→ api/ai-bridge → systems/death → core/save → ui/help → app.js
```

注意：`gate.js` 在 `state.js` **之前**，只依赖 localStorage 与 DOM 桩，不能依赖游戏状态。

---

## 三、核心系统现状（已实现，按功能总览）

### 3.1 角色创建
- 只选性别（♂ 随机男修 / ♀ 随机女修），随机复合身份 = 出身 × 性格 × 名字 × 立绘
- 14 种出身（7 男 7 女），各带专属特性/初始资源/初始功法
- 六维属性（力量/身法/智慧/魅力/心性，1-20）+ 灵根资质（1-100）+ 隐藏气运
- 五行/变异灵根（金木水火土 + 雷冰风光暗），创建时随机并展示
- 性格影响事件选项可用性、NPC 初始好感、判定加成；可缓慢漂移（道心刻度）

### 3.2 修炼与突破
- 闭关修炼：**实时弹窗**（按真实时间推进，7日≈4秒 ~ 一年≈60秒），进度条 + 加成一览徽章，途中随机异动须当场抉择，可提前出关按进度结算；可双人同修（道侣立绘入镜）
- 托管闭关：自动执行到停 / 托管制（有资源消耗，品质越高增长越快）
- 突破：42 档境界（炼气九层 → 筑基/金丹/元婴/化神/炼虚/合体/大乘/渡劫，每大境分前中后圆满 → 仙人），突破条件 = 修为 + 心性判定 + 天劫 + 天地门槛（缺项有面板提示与跳转）
- 突破动画：小境三段 / 大境四段（天劫心魔演出），大境界有天道之劫倒计时与终局战
- 心魔体系：心魔叙事库（30+ 段）、心境状态、劫难节点回溯（回溯符/灵石）、心魔烙印类型化、天劫类型化（天雷/心魔/业火/风劫，天机签预告）
- 寿元系统：每破大境寿命倍增，耗尽身陨；死后可转世重生

### 3.3 探索/战斗/秘境
- 探索 5 大区域（时令影响）+ 奇遇事件池 50+ 条（平静/采药/稀有/史诗/凶险五大类）+ 苦难事件 + 因果链（藏宝图等）
- 战斗：4 战术（抢攻/稳健/龟守/搏命）、五行克制、武器相性/熟练度、战斗弹窗（敌我血条/回合日志/战后统计）、战败机制（切磋重伤 / 野外身陨 / 同伴相护）
- 秘境副本：剑冢/洞府/遗迹/巢穴/残梦多支线 + 隐藏副本信物触发
- 试炼塔：一层一关，每五层统领

### 3.4 宗门
- 8 大宗门（5 正道 + 3 魔道），双轨贡献（贡献点=晋升 / 贡献值=兑换，互不影响）
- 职位：外门→内门→核心→真传→长老→宗主（境界+贡献双达标）
- 宗门任务（含护送商队/营救同门弹窗抉择任务）、宗门人物（传功弟子/长老/掌门 + 随机可结交同门）、拜师、祈福、藏经阁、月俸、宗门大比、宝库兑换
- **开山立派**（已实现）：下山自立事件链、宗门建筑（灵田/丹房/器坊/藏经阁/灵兽园/会客厅）、弟子分工、宗门外交/势力战（事件+判定+声望结算）

### 3.5 人际与道侣（重点，最完善）
- NPC 池 22+ 位（狐仙/剑阁女侠/琴姬/灵药仙子/魔道妖女/龙族公主/白衣剑仙/书仙等，均有独立立绘）
- 关系网：NPC 随机羁绊（同门/旧识/青梅/宿敌/暗恋/挚友/恩怨/师徒），好感联动
- 好感机制：互动增减、好感≤0 交恶成仇（仇视期寻仇）、时间化解
- **道侣系统**：关系阶段状态机（陌路→初见情愫→情愫暗生→两心相知→生死相依→结缡同心）、多轮聊天（5 话题+冷却防刷）、约会（8 地点独立剧情）、心动记忆（8 条）、多幕双修（5 幕演出）、提亲/双修大典、纪念日/昵称、道侣事件（处置不当伤情缘、好感≤20 冷战断缘+仇视、情根深种种情劫）
- 暧昧对象：多线暧昧，修罗场风险（越多越容易触发）
- 结伴同行（战斗照应/探索避险）、打听关系网、牵线搭桥
- **生育/子女传承**（已实现）：道侣同意→孕期胎息弹窗→诞子动画→子嗣继承灵根/属性/性格→培养→转生为子嗣；家族血脉纯度/家谱页

### 3.6 经济/副业/装备
- 坊市 28 种商品、买卖差价、奇珍拍卖、施粥积德、茶棚暗语折扣
- 四大副业：炼丹/炼器/制符/布阵，五行灵根纯度影响副业效率
- 装备强化（最高 +5 失败不降级）、五行武器（火云剑/玄冰刃/庚金剑等）
- 品质分级：凡/灵/宝/仙/神；行囊查看/使用/装备

### 3.7 长线系统
- **轮回点/轮回值**：跨存档全局账户，身陨/飞升结算轮回点，可兑换开局增益（属性/灵根/功法/信物/前世天赋）；轮回值=生涯资历（标题页/仙途录展示）；天道枷锁挑战（多档难度）
- **功德/业力**：显性善恶值 -100~+100，联动性格漂移、宗门外交、天道之劫强度
- **三维声望**：正道/魔道/散修独立累计，影响坊市折扣/拍卖资格/NPC 好感/秘境名额/追杀拉拢
- **四大节日**（按游戏年触发）、**称号墙/存档分享码**、**分魂术/自动化分层**、**AI 叙事桥**（设置页自填 API 地址/Key/模型，兼容 OpenAI 格式，无 key 回退本地规则版）
- 多结局：飞升/堕魔/宗门之主/散修大能/身陨/以身合道/寂灭等，天道终局战 3 阶段

---

## 四、深化文档2 完成状态

- 2A 性格复合身份 ✅ / 2B 灵根共鸣融合 ✅ / 2C 六大流派技能构筑 ✅ / 2D 心魔叙事库与回溯 ✅
- 2E 自动化分层 ✅ / 2F 开山立派 ✅ / 2G 子女传承 ✅ / 2H 轮回点多周目 ✅
- 2I 天道之劫四结局 ✅ / 2J 三维声望 ✅ / 2K 年度规划 ✅ / 2L AI 叙事桥 ✅
- 2M 微操小游戏 ✅ / 2N 目标节奏 ✅ / 2O 因果记忆 ✅ / 2P 数值曲线 ✅
- 2Q 世界回响 ✅ / 2R 分身弟子 ✅ / 2S 称号墙分享码 ✅ / 2T 困难开局 ✅
- 2U 性格档案 ✅ / 2V 素材管线 ✅
- 3A 反馈层 ✅ / 3B 粒子浮动文本 ✅ / 3C 突破演出 ✅ / 3D 叙事界面演出 ✅
- 3E 奖励流水线 ✅ / 3F 音效触感 ✅ / 3G 性能与无障碍 ✅
- **深化文档2 全部可完成目标已落地**；`深化文档.md` 中仍有少量「文档1 遗留待办」（见其「仍待做」清单），可作为后续方向

---

## 五、测试体系（必须会用）

```bash
cd F:\trae-work\trae-play\仙途2
node test\build.js        # 按 index.html 顺序拼接全部 js 到 %TEMP%\xiantu_game.js
node test\run.js          # 运行 smoke1-22 + stress + balance + event（当前 25 项全绿）
node test\audit_links.js  # 链接审计：1014 函数 / 135 引用（重复定义为有意覆盖）
```

**任何改动后必须至少跑 `node test\build.js && node test\run.js`，全绿再交付。**

测试桩关键约定：
- smoke/stress 测试无 `document.body`/`documentElement`：`fxOn()` 自动返回 false → 微操走自动结算路径
- `window._eventModalOpts` + `resolveEventModal(i)` 用于消化弹窗
- 测试会校验数值（修为/灵石/属性/境界）、事件池、平衡性、卡死场景

---

## 六、已修复的坑（改代码时别踩回去）

1. **重复 `<body>`**：曾因拼接 HTML 导致双 body，已修
2. **天道终局战每年重弹**：`tianDone` 守卫防止年度事件重复触发终局战
3. **孕期分手崩溃**：`daoAff` 空值防护
4. **手机布局**：dvh 视口、safe-area、iOS 输入 16px 防放大、触控目标 44px
5. **事件必选**：遇到事件必须选择后才可继续（用户明确要求，别改成可跳过）
6. **好感防刷**：交谈/切磋/赠礼有冷却，不允许同事件快速刷好感
7. **双轨贡献**：贡献点（晋升）与贡献值（兑换）独立，购物不降地位
8. **心性突破提示**：突破缺心性时有跳转地点功能（心魔历练等路径）
9. **时间机制**：按游戏通关时间适配，每日/每周任务周期已压缩，勿拉长

---

## 七、部署与更新流程

### 7.1 线上状态
- GitHub Pages 已开启，`https://zhou-ck.github.io/xiantu/` 线上可玩
- 邀请码门禁生效（`xiantu2026`），首页/manifest/sw.js/gate.js/图标均验证 200

### 7.2 更新发布

```bash
cd F:\trae-work\trae-play\仙途2
git add .
git commit -m "更新说明"
git push
```

1-2 分钟后线上自动更新。**git 命令需要申请提权**（沙箱外执行），仓库本地已配好代理 `http://127.0.0.1:7897`（GitHub 直连超时）。

### 7.3 换邀请码
改 `js/core/gate.js` 的 `INVITE_CODE` + `sw.js` 的 CACHE 版本号 → 推送即可。

### 7.4 其他托管方案
见 `部署指南.md`：Netlify 拖拽即上线、Cloudflare Pages（国内访问相对友好）、Cloudflare Access 邮箱白名单（真·权限控制，免费 50 人）、localtunnel 临时隧道。

---

## 八、开发铁律与约束

1. **只改 `仙途2\`**；不动旧版单文件 `仙途-手机版.html`、不动 `仙途\`、`仙途-app\`
2. **脚本加载顺序**按 index.html 严格执行（见 2.3），新增脚本插对位置
3. 纯前端 + localStorage 存档，**不要引入需要后端的架构**（除非用户明确要求云端存档）
4. 素材新增必须登记到 `assets/LICENSES.md`（版权台账），用免费可商用渠道
5. 移动端优先，任何新 UI 必须适配小屏（44px 触控目标、dvh、safe-area）
6. 文案风格：古典仙侠文风，人物必有名字；界面文字精炼
7. 数值设计要防「闭关到顶」类枯燥通关：已有收益递减、托管资源消耗、境界门槛、随机事件打断；新增机制保持同类约束
8. 用户偏好：**体验感优先**（弹窗、动画、演出、立绘），拒绝"点一下就完成"的粗陋交互；事件不可跳过；关系/感情线要有温度

---

## 九、后续方向参考（按深化文档遗留 + 用户关注点）

- `深化文档.md` 的「仍待做」清单：功法技能化深化、敌人 AI 多样化、BOSS 守关、事件链/时代主线/伏笔表、洞府房间化、本命法宝/套装、灵兽扩充、排行榜、App 打包（Capacitor）
- 用户已提过：**App 版开发**（Capacitor 打包骨架在 `../仙途-app`，未启用）；云端存档/换设备不丢进度
- 用户关注「游戏趣味性与功能增强」>「数值平衡」，新功能要能感知、有演出、有选择

---

## 十、给新对话的第一句话建议

新对话可以直接粘贴：

> 请先阅读 `F:\trae-work\trae-play\仙途2\接管文档.md`、`深化文档.md`、`深化文档2.md`、`功能总览.md`，然后运行 `node test\build.js && node test\run.js` 确认基线全绿，再根据我的需求开发。

