# 仙途 · 机制问题诊断与改进建议

> 更新日期：2026-08-06
> 定位：机制层面的**问题清单 + 改进意见**，供后续开发（Phase B/C/D）落地时取舍。
> 与既有文档的关系：《功能总览.md》讲"有什么"，《长期规划.md》讲"往哪走"，《继续开发档案.md》讲"做什么"——本文档讲"哪里不对、怎么改"。
> 结论先行：**机制底盘扎实、乘区体系完整，大方向问题集中在「后期数值节奏」「玩法间收益结构」「随机与挫败感」「内容与测试工程化」四块。**

---

## 一、问题总览（按优先级）

| # | 问题 | 一句话 | 优先级 | 改动成本 |
| --- | --- | --- | --- | --- |
| 1 | 后期奖励与需求指数脱节 | 后期只有闭关在产出，其他系统成装饰 | **P0** | 低（改系数） |
| 2 | 离线收益与放置定位冲突 | 40% 离线惩罚逼玩家"挂着" | **P0** | 极低 |
| 3 | 流派是正则判定，非设计 | 改功法名流派就崩，无择道节点 | P1 | 中 |
| 4 | 五行克制感知弱 + 战斗 10 回合强平局 | 克制无法主动选择；龟守流打不死强敌 | P1 | 中 |
| 5 | 随机致死 vs 长进度 | 无保命机制，一次身陨数小时进度归零 | P1 | 低 |
| 6 | 灵石后期无出口 | 经济单边膨胀，回收手段不足 | P1 | 中 |
| 7 | 内容散落代码 | 数据表化滞后，AI 桥无从接入 | P1 | 高 |
| 8 | 测试桩与真实环境鸿沟 | 时序/动画类 bug 测不到 | P2 | 中 |
| 9 | 负反馈不透明 | 收益递减/惩罚只有一行字 | P2 | 极低 |
| 10 | 后期小境界节奏拖沓 | 渡劫期一层 ≈ 1200 游戏天纯等待 | P2 | 低 |

---

## 二、详细问题与改进意见

### 问题 1 · 后期奖励与需求指数脱节（长草期根源）【P0】

#### 现状证据

- 境界阈值（`js/core/world.js` 的 `THRESHOLDS`）为**亚指数增长**：炼气期 100→800（每层 ×1.12），筑基 1000→3000（×1.25），金丹 4250→8000（×1.23），元婴 11000→20000（×1.22），化神 27500→50000（×1.22），渡劫期 625000→1000000（×1.17）。
- 而玩法奖励几乎全是**线性或固定值**：
  - 战斗胜场：`rand(20,80)+rl()*8`（`combat.js`），合体期 `rl()≈25` → 单场约 280 修为，不到当层阈值（62500）的 0.5%
  - 试炼塔：`40+floor*25+rl()*8`（`explore.js` 的 `doTower`），同样线性
  - 闭关异动事件：`cultGain` 基数为 `40+S.root/3`（≈55）+ 境界系数 `1+bigStage*0.25`，合体期约 ×3 → 单事件 160 左右
  - 事件池（`content.js`）给的修为多数是写死的固定值
- 综合：**后期 99% 的修为来自闭关**，探索/战斗/秘境/任务/事件全部沦为"点缀"，玩家只剩「闭关 → 等 60 秒 → 出关」一条路。这是"长草期"的真正数值根源，也是《长期规划》G1/G2 想解决、但没从根源动过的地方。

#### 影响

- 后期玩法多样性归零 → 留存流失（对应长期规划北极星"30 日留存 ≥10%"的硬伤）
- 战斗/秘境/试炼塔"做了但不值得打"，系统连接原则（≥2 旧系统）失效

#### 改进意见

1. **奖励指数化**：把修为奖励从 `rl()*8` 改为按大境的档位系数，例如 `rl()*rl()/10*档位` 或直接给每大境一个基准表 `BIG_GIFT=[100,300,800,2000,5000,12000,30000,70000,150000]`，让后期一场硬仗 ≈ 当层阈值的 3-8%。
2. **秘境/试炼塔/妖潮守城复用同一系数函数**（抽一个 `realmGift(stage)`），一处改全盘生效。
3. **需求端微调**：渡劫期小境增幅 1.17 → 1.10，配合效率端给"后期加速手段"（见问题 10）。
4. **验收**：在 `test/balance.js` 增加断言——合体期后"战斗+探索"合计收益占当层阈值 ≥2%（可在仿真里统计）。

---

### 问题 2 · 离线收益与放置定位冲突【P0】

#### 现状证据

- `applyOfflineGain`（`js/core/save.js` 或相关处）：离线按 40% 效率结算，上限 12 小时。
- 《长期规划》自述定位为「不强制社交、每天 20-30 分钟的保底型放置 PWA」，对标《一念逍遥》的"放置自觉"——玩家**走了会回来**。

#### 影响

- 40% 离线惩罚 = 离线玩家每天损失 60% 潜在收益 → 玩家被迫"挂着游戏"，与"放置自觉"完全相反。
- 离线收益低于在线挂机，等于变相鼓励霸屏挂机，也不利于省电（PWA 场景）。

#### 改进意见

1. **离线效率提到 60-80%**，截断逻辑从"12 小时硬截断"改为"离线收益上限 = 12 小时等价"（超过后继续累积但不再增长，回归时按上限结算）。
2. 托管闭关（`_cult.trust` + `autoRules`）扩成**离线托管位**：离线时按托管策略自动购丹/自动修炼到停，回归弹窗明细——这本身就是规划中"月卡卖点"的零成本预演。
3. **验收**：`test/` 增加离线结算断言（离线 24h 收益 = 12h 上限、效率系数在 0.6-0.8）。

---

### 问题 3 · 流派是正则判定，不是设计【P1】

#### 现状证据

- `flowType()`（`js/systems/mechanics.js`）用正则匹配功法名/武器名/职业字符串判定流派：`/剑|锋|青锋|太乙|斩/.test(art)`、`/体|淬|金刚|不灭/.test(art)` 等。
- 后果：
  - 功法名改动即失效（`ARTS` 表里 `心魔淬体功` 会被判成体修，`丹火诀` 会被判成丹修——其实这些判定有大量误判空间）
  - 玩家在创建/入宗/学功法时**没有任何"选择流派"的显式节点**，六流派（剑/魔/体/丹/鬼/法）只是战斗里的一串隐式加成
  - 流派加成仅作用于战斗（`flowCombatBonus`），不作用于修炼/副业/事件判定，存在感极弱

#### 影响

- 规划里"单次轮回中期选择 ≥5 个"的目标目前没有真正落地——现有系统虽多，但几乎都是"解锁"而非"取舍"，重开一局的路径没有本质差异。

#### 改进意见

1. **数据驱动化**：`ARTS` 每条加 `flow` 字段（sword/demon/body/dan/spirit/law），`flowType()` 改为查字段优先、正则兜底。
2. **择道节点**：筑基成功时（或首次入宗门时）弹出**二选一/三选一**的显式择道（如：剑修「以杀证道」/体修「肉身成圣」/法修「五行通玄」），不可逆或代价高（可花轮回点重选）。
3. **流派贯通**：流派同时影响修炼（如剑修战斗得修为加成、丹修炼丹造诣门槛低、体修苦修收益高）与副业——把 `flowCombatBonus` 扩展为 `flowBonuses(s)` 返回多领域加成，并在面板展示"当前流派"徽章。
4. **验收**：新增 smoke 断言——每种功法显式 flow 字段可达、无正则误判；择道节点在筑基流程可达。

---

### 问题 4 · 五行克制感知弱 + 战斗 10 回合强制平局【P1】

#### 现状证据

- 克制只有伤害系数：克制 ×1.25、被克 ×1.15、武器相性 +1（`mechanics.js`），且**敌人五行在开战前就公示**，玩家只能"看着挨克"，没有主动选择空间。
- `combat.js` 的 `step()`：`if(rnd>10){st.rounds=10;finish(false,true);return}`——**10 回合无条件判平**。龟守/高防流在血量悬而未决时被强制平局，且平局奖励极低（`doTower` 平局只给 `10+floor*5` 灵石）。

#### 影响

- 克制数值存在但体验为零：玩家不会因为"选对五行"获得正反馈。
- 10 回合强平局对"消耗流"（龟守、毒、反伤）build 是隐性死刑——防守流派被机制性削弱。

#### 改进意见

1. **克制主动化**：开战前（`startCombat` 的选项区）给"克制预览"：显示敌人五行与我方可换装备/功法的克制关系；探索前可依据区域情报预选克制武器。
2. **克制正反馈**：克制命中时额外掉落（+10% 灵石/材料）或触发专属演出文本，让"选对"有可感知收益。
3. **平局规则软化**：10 回合后改为"每回合双方伤害 ×1.2 的加时赛（最多再 5 回合）"，仅在双方均无法击杀时才平局；或允许龟守流在 10 回合后选择"撤退"并保留 60% 战利品（已耗时间不白费）。
4. **验收**：战斗 smoke 增加"龟守流 vs 高防敌可获胜或撤退"的用例。

---

### 问题 5 · 随机致死 vs 长进度，缺保命与惩罚保底【P1】

#### 现状证据

- 致死路径多且随机：走火入魔（`heartTraining` 判定失败直接 `die('走火入魔')`）、苦难事件、野外战败、心魔反噬、渡劫失败（倒退 10-30% 修为 + 心魔烙印）。
- 一局进度是数小时起的（仿真：金丹中位 600-4000 游戏天），死亡即轮回，只保留"部分灵根与记忆"。
- 已有兜底：同伴拼死相护（55% 概率）、崩溃恢复存档——但**没有玩家可主动持有的保命手段**。

#### 影响

- 随机性致死 × 长进度 = 最大挫败感来源；"苦修"按钮（效率 +40% 但每次有 22% 概率心魔 +1）在后期几乎没人敢碰，高风险玩法被设计性劝退。

#### 改进意见

1. **保命道具**：坊市出售"替身傀儡/保命符"（可持有 1-2 个），战败致死/走火入魔时自动消耗、以"重伤濒死"结算替代死亡；这同时是灵石回收出口（见问题 6）。
2. **突破惩罚保底**：渡劫失败时，除修为倒退外给"下一次突破 +5% 成功率"的保底（类似已有"渡劫感悟"机制但显式化、可叠加），符合《长期规划》设计纲领第 10 条"随机要有边界"。
3. **高风险高回报显式化**：苦修按钮旁明示"走火概率 ≈22%"与当前保命符持有数，让玩家知情决策。
4. **验收**：smoke 覆盖——持有保命符时致死路径一律转为重伤；渡劫失败获得保底标记。

---

### 问题 6 · 灵石经济后期无出口【P1】

#### 现状证据

- 灵石产出面广（战斗/任务/探索/坊市差价/月俸/里程碑），消耗面窄（购物、托管丹药、提亲彩礼 500、强化）。
- 后期随境界增长，灵石收入按 `rl()` 微涨而消耗基本固定 → **灵石线性溢出**。功能总览已提及"经济与资源隔离"，但缺"出口表"。

#### 影响

- 货币失去价值 → 坊市/拍卖/施粥等经济玩法后期全部变成摆设；也失去一个有效的难度调节旋钮。

#### 改进意见

1. **出口表（对应《继续开发档案》Phase C"资源经济出口表"）**：为灵石列出 3-4 个持续出口——保命符、托管修炼位、洞府装饰/设施升级、宗门捐资换贡献值。
2. **奇珍拍卖升级**：后期拍卖池加入稀有品（功法残页/强化石/灵宠蛋），拍卖价按玩家灵石存量动态上浮，形成回收。
3. **验收**：`test/balance.js` 增加"5000 游戏日后灵石存量收敛（不无限膨胀）"的仿真断言。

---

### 问题 7 · 内容散落代码，数据表化滞后【P1】

#### 现状证据

- `js/core/content.js` 仅 247 行；大量叙事/事件文本内嵌在 `social.js`（975 行）、`romance.js`（777 行）、`sect.js`（611 行）、`explore.js`（721 行）等系统文件里。
- 后果：跨文件查找困难、无法热更（OTA 无从谈起）、AI 扩写桥（`ai-bridge.js` 仅 42 行）只能做对话润色，接不进内容生产。

#### 影响

- 内容扩充（G1 目标 500→3000 条）在当前结构下每批都要动代码、过脚本顺序关，速度上不去；《继续开发档案》Phase C 的数据表化是正解，但建议**提前启动地基**。

#### 改进意见

1. **先做 schema 与校验器，再搬内容**：从"社交事件"（当前最大最热的一块）开始抽 JSON，不要等系统全部开发完再统一搬（避免大爆炸式重构踩脚本顺序雷）。
2. 数据表加载器与现有 `world/content/persona` 兼容：`DATA.events['social']` 等命名空间，测试里 `audit_links` 同步校验数据完整性（0 断链/0 undefined）。
3. **AI 桥接内容管线**：ai-bridge 增加"生成 → schema 校验 → 人工审核入库"的本地模式（复用 Phase C 计划）。
4. **验收**：第一批 50 条社交事件搬入 JSON 后，`node test/audit_links.js` 依旧全过。

---

### 问题 8 · 测试桩与真实环境鸿沟【P2】

#### 现状证据

- `test/` 体系用 `vm` 沙箱 + 桩 DOM（`makeEl`），且 `setTimeout: fn=>fn()` 同步化——时序、动画、弹窗层叠、`requestAnimationFrame` 类逻辑基本测不到。
- 功能总览里连续多批"手机端修复"（弹窗遮挡、血量归零卡死、战斗小屏滚动等）就是这类 bug 反复出现的证据：**修得快，但也反复出现**，说明回归面不够。

#### 改进意见

1. 引入 **Playwright 无头 E2E**：真实浏览器跑 `index.html`，覆盖 5 条关键路径——创建角色 → 闭关（含中途异动抉择）→ 突破 → 战斗（含战败/平局）→ 存档读档。
2. CI（`.github/workflows/ci.yml`）在 build+run+audit 后追加 E2E 步骤（`npx playwright test`，浏览器缓存进 CI artifact）。
3. 关键弹窗（panel/battle/cultivate/breakthrough/ending）加 `data-testid` 或稳定选择器，避免 E2E 脆弱。
4. **验收**：E2E 5 条路径在 CI 全绿；此后"手机端修复批次"类 bug 数量下降。

---

### 问题 9 · 负反馈不透明【P2】

#### 现状证据

- 收益递减（`streakDiminMult`）：连续闭关 ≥60 日逐降，面板只有一行 `⚠️ 你已连续闭关 X 日`，玩家不知道现在实际 ×0.87 还是 ×0.62。
- 惩罚类机制（苦修走火概率、突破失败损失、心魔后果）多数不显式展示概率/数值。

#### 改进意见

1. 修炼面板进度条旁实时显示"当前收益 ×0.87"，递减分段明示（`×0.9 → ×0.8 → ×0.7 → ×0.6 → ×0.5` 各在多少日触发）。
2. 高风险按钮统一加数值标注：苦修按钮"走火 ≈22%"，心魔历练按钮"失败可致死"。
3. **验收**：文案类改动，人工走查 + 现有 smoke 全绿即可。

---

### 问题 10 · 后期小境界节奏拖沓【P2】

#### 现状证据

- 由问题 1 的数值推演：合体/渡劫期效率乘区基本堆满（≈×6-8），而需求仍 ×1.17/小境递增 → 后期每层小境界 ≈ 1000+ 游戏天纯闭关等待（换算真实时间 3-4 分钟/层，且收益只来自闭关）。
- 蒙特卡洛仿真断言"金丹中位 600-4000 天"也印证后期显著放缓。

#### 影响

- 后期"每层进度感"严重衰减，玩家在合体/大乘期最易流失（放置游戏的经典流失点）。

#### 改进意见

1. **需求端**：渡劫期小境增幅 1.17 → 1.10（配合问题 1 一起调）。
2. **大境界质变反馈**：每破一个大境给一次"质变对比演出"（已部分实现），并明确开放新内容（对应《继续开发档案》Phase B"境界内容开放表"）——让后期每个大境都有"新东西"，而不只是数字变大。
3. **三时间尺度落地**（规划纲领第 5 条）：每周目标（试炼塔 X 层/秘境 1 座）与"本周加速 buff"挂钩，给后期玩家"周节奏"而非"日复一日闭关"。
4. **验收**：仿真断言——渡劫期每层中位耗时 < 400 游戏天（当前约 1000+）。

---

## 三、实施顺序建议

```
第一刀（本次/近期，P0，均为系数级改动，半天内可完成+回归）：
  问题 1（奖励指数化）+ 问题 2（离线收益）+ 问题 10 的需求端微调
    → 改完立即跑 balance.js 仿真对比，预期：后期非闭关收益占比回升、渡劫期层耗下降

第二刀（Phase B 期间，P1）：
  问题 3（流派数据化+择道节点）→ 问题 5+6（保命道具+灵石出口，一石二鸟）
    → 问题 4（克制主动化+平局软化）

第三刀（Phase C/D 期间，P1-P2）：
  问题 7（schema 先行搬社交事件）→ 问题 8（Playwright E2E）→ 问题 9（反馈透明化）
```

## 四、与现有规划的衔接说明

- 问题 1/6/10 与《长期规划》G1/G2、《继续开发档案》Phase B/C 的"内容扩充/经济出口/境界开放表"**目标一致，但本文档给出了具体系数与验收断言**，建议把这些断言并入 `test/balance.js`。
- 问题 7 完全承接 Phase C 数据表化，仅建议"提前到 Phase B 并行启动地基"。
- 问题 8 是新增建议（现有文档未提 E2E），成本可控、收益明确，建议排进 Phase A 之后的工程收尾。
- 问题 2 的"离线托管位"与《长期规划》第七章月卡卖点直接衔接，属零成本预演。

---

## 五、待确认/待决策点

1. **后期加速的取舍**：需求端调低（1.17→1.10）会缩短单局时长（北极星"单局 3-7 天前期/2-4 周后期"仍是游戏内时间），是否接受？倾向：接受，配合内容扩充对冲。
2. **择道节点代价**：重选流派是否允许（花轮回点）？倾向：允许但高价，保留重开价值。
3. **E2E 引入**：是否同意引入 Playwright 依赖（打破"无 npm 依赖"现状，仅 devDependency，不影响运行时）？倾向：同意，只作 devDependency。
