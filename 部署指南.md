# 仙途 · 免费托管部署指南

> 目标：把 `仙途2` 这个 PWA 部署到免费 https 托管，手机打开即可全屏安装、离线游玩；以后更新只需推送代码，自动发布（一劳永逸）。
> 本项目是纯静态站点（HTML/CSS/JS/图片），任何静态托管都能直接跑。

## 一、GitHub Pages（推荐 · 推送即发布）

免费、https、支持 PWA 安装、更新走 git push 自动发布。

### 1. 电脑本地（已由 Codex 初始化）

```bash
cd F:\trae-work\trae-play\仙途2
git add .
git commit -m "仙途 PWA 初始版本"
```

### 2. 创建 GitHub 仓库（网页操作，约 2 分钟）

1. 打开 https://github.com/new（没有账号先注册）
2. Repository name 填 `xiantu`（或任意英文名）
3. 选 **Public**（免费）
4. 不要勾选任何初始化选项（README/.gitignore 都别勾，否则要处理合并）
5. 点 Create repository
6. 复制页面显示的仓库地址，形如：
   - HTTPS：`https://github.com/你的用户名/xiantu.git`

### 3. 关联并推送（把地址换成你自己的）

```bash
git remote add origin https://github.com/你的用户名/xiantu.git
git branch -M main
git push -u origin main
```

推送时会弹出 GitHub 登录窗口，按提示完成即可。

### 4. 开启 Pages

1. 仓库页 → **Settings** → 左侧 **Pages**
2. Build and deployment → Source 选 **Deploy from a branch**
3. Branch 选 **main**，目录选 **/ (root)** → Save
4. 等 1-2 分钟，页面顶部会显示网址：
   `https://你的用户名.github.io/xiantu/`

### 5. 以后更新

每次改完游戏，执行：

```bash
git add .
git commit -m "更新内容"
git push
```

1-2 分钟后线上自动更新。手机上打开网址 → 浏览器菜单「添加到主屏幕」→ 全屏 App 体验。

---

## 二、Netlify（最简单 · 拖拽即上线）

适合「先快速给别人玩」，不需要命令行：

1. 打开 https://app.netlify.com/drop
2. 把 `F:\trae-work\trae-play\仙途2` 文件夹直接拖进网页
3. 立刻得到 `https://xxx.netlify.app` 网址，手机直接打开
4. 想长期保留：注册账号后，把站点 Claim 到账号下
5. 以后更新：重新拖拽文件夹，或连接 GitHub 仓库实现自动部署

---

## 三、Cloudflare Pages（免费额度大 · 国内访问相对友好）

1. 注册 https://dash.cloudflare.com
2. 左侧 **Workers & Pages → Create → Pages → Upload assets**
3. 上传 `仙途2` 文件夹内容，命名项目（如 `xiantu`）
4. 得到 `https://xiantu.pages.dev` 网址
5. 以后更新：直接再拖拽上传，或连接 GitHub 自动部署

---

## 四、手机端使用

- 手机浏览器打开部署后的 https 网址
- **添加到主屏幕**：Chrome 菜单 →「添加到主屏幕」；Safari 菜单 →「添加到主屏幕」
- 之后从主屏幕图标打开：全屏、带图标、离线可玩（PWA）

## 五、常见问题

- **为什么本地 `http://192.168.x.x` 不能安装 App？** 浏览器要求 https 安全上下文才注册 Service Worker（离线/安装）。部署到上面任一免费 https 平台即可解决。
- **路径报 404？** 本游戏全部使用相对路径，GitHub Pages 子路径（`/xiantu/`）也能正常访问。
- **更新后手机上还是旧版？** PWA 会缓存，清一次浏览器缓存或重新添加到主屏幕即可；每次发布后缓存版本会自动轮换（sw.js 内 CACHE 版本号）。
